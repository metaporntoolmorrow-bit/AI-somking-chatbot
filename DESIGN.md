# แนวทางส่วนติดต่อผู้ใช้

อ้างอิงภาพ `Screen Short design` ที่ให้มากับโปรเจคต์: พื้นสีเขียวเสจ ภาพรวมอบอุ่น อ่านง่าย การ์ดสีขาว ขอบมน ปุ่มทรงแคปซูล และข้อความภาษาไทยที่ชวนลงมือทำ

- สีหลัก `#23442f` สำหรับตัวอักษรและปุ่มสำคัญ, สีพื้นฮีโร่โทน `#bedb9d` ถึง `#d9ecc3`, พื้นหน้า `#f7f8f2`
- สีเขียวอ่อนสำหรับข้อมูลพร้อมใช้ สีเหลืองสำหรับกรณีมีเฉพาะเครื่องมือเสริม และสีส้มอมแดงสำหรับช่องว่างในคลัง
- ลำดับสายตา: เป้าหมายของผู้ใช้ → การถามบริบท → ผลลัพธ์ → เครื่องมือ → ขั้นตอนลงมือทำ → การส่งต่อ/ดาวน์โหลด
- ตัวเลือกในแบบถามปรากฏตามชุดสถานการณ์ที่มีจริง ไม่แสดงตัวเลือกที่สร้างแผนไม่ได้
- หน้าเว็บไม่แสดงข้อความสถานะจำลองตามคำขอล่าสุดของเจ้าของโครงการ; เก็บสถานะจริงไว้ในฐานข้อมูลและคงไฟล์ต้นฉบับไว้ตามเดิม
- รองรับจอเล็กด้วยกริดหนึ่งคอลัมน์ ปุ่มและช่องเลือกขนาดสัมผัสได้ และการพิมพ์หน้าแผน

## ภาษาที่ใช้บนหน้าเว็บ

- ใช้ “ชุดแผน” แทนคำเรียกชุดกิจกรรมทางเทคนิค และ “ข้อมูลชุดเครื่องมือ” แทนชื่อชุดข้อมูลภายใน
- ซ่อนรหัสอ้างอิงของสถานการณ์ ชุดแผน และเครื่องมือจากหน้าที่ผู้ใช้เห็น โดยคงรหัสไว้ในข้อมูลภายในเพื่อจับคู่ได้ถูกต้อง
- ชื่อเครื่องมือที่เป็นภาษาอังกฤษแสดงเป็นคำไทยที่เข้าใจหน้าที่ของเครื่องมือ ส่วนชื่อเดิมอยู่ในเอกสารต้นทาง

## หน้าผลลัพธ์และไฟล์พิมพ์

- หน้าผลลัพธ์ยึดภาพ `ไฟล์ตัวอย่างหน้าสุดท้าย/ตัวอย่างหน้าสุดท้ายในเว็บ Chatbot.jpg`: สถานการณ์ เครื่องมือหลัก/เสริม แผน 4 ช่วง และปุ่มไฟล์อยู่ในหน้าเดียว รายละเอียดเต็มเปิดดูเมื่อจำเป็น
- ไฟล์พิมพ์ยึดภาพ `ไฟล์ตัวอย่างดาวน์โหลด/ตัวอย่างไฟล์ดาวน์โหลด 1.jpg`: หน้า A4 แนวนอน แบ่งหัวเรื่องกับบริบท การ์ดเครื่องมือ แผน 4 ช่วง ความเสี่ยง เงื่อนไขความสำเร็จ และการติดตามผล พร้อมหน้ารายละเอียดต่อท้าย
- การ์ดเครื่องมือที่ดาวน์โหลดจากเว็บใช้ฉบับภาษาไทยใน `public/tools/` และไม่แสดงรหัสอ้างอิงบนตัวเอกสาร โดยเก็บไฟล์ต้นฉบับไว้แยกต่างหาก
